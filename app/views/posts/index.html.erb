<% provide :title, "Posts index" %>
<h1>Posts index</h1>

<div class="row">
	<ul id="posts-index">
		<% @posts.each do |post| %>
			<li>
				<%= post.title %>
				<% if logged_in? %>
					| <%= post.user.name %>
				<% end %>
			</li>
		<% end %>
	</ul>
</div>